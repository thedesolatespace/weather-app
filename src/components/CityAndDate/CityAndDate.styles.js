import styled from 'styled-components';

const StyledCityAndDate = styled.div``;
const City = styled.h2`
  margin-bottom: 10px;
  font-size: 40px;
  font-weight: 700;
`;
const CurrentDate = styled.p``;
export { City, CurrentDate, StyledCityAndDate };
